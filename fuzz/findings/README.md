# Findings

Reproduced real bugs in Vinyl, each labelled by status. A previously-known issue
re-reported as new adds no value, so every entry states whether it is NOVEL or a
CONFIRMATION of a known (source-reviewed) issue, with the three-way reference
behaviour and a minimized reproducer.

| dir | status | RFC clause | one line |
|---|---|---|---|
| `channel-truncation-CONFIRMED` | CONFIRMATION | RFC 9639 §9.1 | decoder returns fewer channels than STREAMINFO declares (recombine `zipWith` truncation); libFLAC rejects, Vinyl accepts→1ch |
| `samplerate-zero-CONFIRMED` | CONFIRMATION | RFC 9639 §9.1.7 | STREAMINFO sample rate 0 accepted with audio; `readStreamInfo` does not check |
| `encoder-stack-overflow-CONFIRMED` | CONFIRMATION + **FIXED** | robustness | encoder non-tail recursion overflows the stack. Per-SAMPLE loops fixed 2026-08-30; then `fz_encode_stack` autonomously found FOUR more beyond C04's list — per-FRAME `writeFrames`/`chunkChannels`, per-sample `Fixed.diff1`/`Lpc.residualAux`, and the dominant `Heuristics.partitionSearch` `List.sum` (foldr) — all **fixed** 2026-08-31 (TR+`@[csimp]` / `foldl`, gate green). Encoder now stack-safe to a 128 KB stack across the checked block range; the prober is a clean regression guard. **Correction 2026-09-11:** the `writeFrames` remediation was recorded here as clean, but its accumulator appended at the *tail* (`acc ++ frame`), which Lean compiles to the reverse-based `appendTR` — so it traded the O(n) stack for **O(n²) time**, `Θ(E·F)` in emitted bits over frames. Predicted in `new-findings.md` §5.1 and never measured; it survived 10.8 B executions because no oracle treats slowness as failure, surfacing only as 248 `blockSize=16` slow-unit artifacts. Fixed 2026-09-11 (`writeFramesRev` accumulates via `List.reverseAux` and reverses once; `writeFramesRev_eq` + the same `@[csimp]`): 14.8 s → 0.80 s on the worst witness, doubling frames now costs 1.97× (was 5.83×), `overflow=0` still holds |
| `emit-streaminfo-bps-below-4` | **NOVEL** + **FIXED** | RFC 9639 Table 3 | `Audio.WellFormed` required only `1 ≤ bps`, so `encodeChecked`/`encodeCheckedCfg` accepted bit depths 1–3 and emitted a STREAMINFO both reference decoders reject (`flac`: "bits per sample is 3, must be 4-32"; `ffmpeg`: "invalid bps: 3") while Vinyl decoded its own output — the round-trip capstone holding over artifacts that are not FLAC. `readStreamInfo` performed no bit-depth validation either, so the decoder accepted them too. Detected 11,532× by `fz_emit_conformance` and catalogued, never triaged. Fixed 2026-09-11: `4 ≤ bps` in `Audio.WellFormed` (encoder) and a Table 3 guard in both `readStreamInfo` twins (decoder); `bps_bad` 140 → **0**, IETF must-decode still 61/61 |
| `decoder-output-contract-stereo` | CONFIRMATION (§1.1 gap) | RFC 9639 §4.2 / output-contract | stereo decorrelation undo is not wrapped to `bps`, so `decodeArrays` returns samples in `FitsSInt(bps+2)` not `FitsSInt(bps)`; libFLAC rejects, Vinyl accepts→out-of-range→two's-complement-wrapped PCM |
| `decode-capacity-underalloc` | CONFIRMATION (missed-lead #1) | capacity / P3 class | `decodeBytes` output pre-size hardcodes 2 bytes/sample; every 24/32-bit decode reallocs (short by `(⌈bps/8⌉−2)/⌈bps/8⌉`). Proof-invisible; `underalloc=109,679` in the 6h run |
| `float-lpc-coefficient-divergence` | CONFIRMATION (§1.4 / F5b) | claim-surface / quality | Float LPC search selects a different quantized coefficient vector than exact arithmetic above 2⁵³ (24/32-bit); round-trip still holds. `float_exact_divergence=517` |
| `overlong-coded-number` | CONFIRMATION + **FIXED** | RFC 9639 §9.1.5 / RFC 3629 | `readUtf8` accepted non-minimal (overlong) coded frame numbers (UTF-8 overlong class). **Fixed 2026-08-31** (`Utf8Num.contsFloor` minimality gate in both reader twins, gate green); `fz_overlong_utf8` flipped to a two-way regression pin (`overlong_accepted` 114M→0) |
| `decoder-stack-overflow-readriceseq` | NOT A SHIPPED BUG (corrected) + reference model **FIXED** | robustness | **2026-09-02:** `fz_proven_pairs` (12 h campaign) overflowed the reference model's per-sample LPC restore `Lpc.restoreAux` (non-tail, depth = the *declared* block size — a 3343 B malformed input the production decoder rejects); fixed with the `restoreAuxTR` `@[csimp]` twin, repro `repro-restoreAux-3343B.flac`. Earlier: the non-tail `Rice.readRiceSeq`/`readSIntSeq` (`x :: rec`) are on the REFERENCE decoder path only (`decodeReference`→`Subframe.read`→`Rice.readResidual`); the SHIPPED array decoder (`decodeArrays`/`decodePcm16A`) uses the already-tail `Flac.Decode.readRiceSeqScan`/`readSIntSeqGo` and calls no `Flac.Rice.readRiceSeq*` (IR-verified). Earlier drafts misattributed a `vm_decode_ref` gdb trace to the shipped decoder. The `readRiceSeqTR`/`readSIntSeqTR` `@[csimp]` twins landed anyway (sound, theorems intact, pinned in `check.sh`) as a minor reference-path hardening; `fz_decode_stack` verifies the shipped decoder is stack-flat |
| `decode-sample-divergence-highbps` | TRIAGED — accept-set, NOT a bug; rig **discriminator built** | RFC 9639 §5 (unspecified on invalid data) | four **bps=31** mid/side witnesses (`repro-1003B`, 2026-09-03, was auto-classified by the tool: frame 2 mid local[8] ≈ −2^37). **2026-09-02 (`repro-215B-b`)**: same mechanism, exact reconstruction ≈ −2^40; `flac_reconstruct_oob_at` (`common/flac_residual.c`) now proves from the bitstream alone that a subframe leaves its coded depth before the divergent sample, so `wide_diff.c` catalogues the class (`wide_sample_diff_oob_coded`) instead of aborting — sound (cannot fire on a valid stream), validated against Vinyl's own fold and the 561B hand analysis. **215B**: malformed — frame sample rate 88200 contradicts STREAMINFO 44100; the libFLAC CLI rejects it, but the rig's lenient linked referee accepted it and formed a false consensus. Fixed: `wide_diff.c` `fw_write` now rejects the sr contradiction (`fw.sr_mismatch`), matching the CLI → 215B is now a non-fatal `_1ref` entry. **561B**: root-caused (two independent analyses, exact arithmetic) to `Lpc.restoreA`'s coded-depth `wrapSInt b`: a mid sample reconstructs out of the coded 31-bit range (valid 32-bit), Vinyl wraps to 31 bits, libFLAC/ffmpeg keep int32 → Δ=2^23. RFC 9639 §5 leaves out-of-coded-range decode unspecified; Vinyl's wrap is the P1 output-contract hardening and is safer. No codec fix; same class as `decoder-output-contract-stereo`. Recorded as a known accept-set witness |

The output-contract entry is the highest-value class the `official` campaign
tripped: the decoder has **no output contract**, confirmed on both a generated
`b+2` construction and a mutated *real* 16-bit stream. It is a tier correction
(P1 is marked **T** but the boundedness holds only at subframe depth) and P7's
mirror (`Flac.encode (decode bytes) = none` on a stream Vinyl accepted), not a
soundness break — see its `NOTE.md`. The remaining catalogue classes
(`wide_ref_split`/`_disagree` = referee-vs-referee, `a_only`/`b_only` = mutator
accept-set noise) are manufactured, not Vinyl defects.

## How these were produced

Divergences dump to `runs/<ts>/<label>/divergences/<class>/` during a campaign;
curated reproducers are promoted here with a NOTE. The `FUZZ_STRICT>=2` escalation
turns the corresponding catalogue into an `abort()` for regression-pinning a filed
witness.
